package ua.com.fielden.platform.dashboard;

/** A general interface for all dashboard item results. */
public interface IDashboardItemResult {

    public enum StateColour {
        RED, YELLOW, GREEN
    }

}
