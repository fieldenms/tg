package ua.com.fielden.platform.tiny;

/// Identifiers for actions provided by the platform.
///
/// To reduce the likelihood of name clashes with applications, all identifiers defined here start with `PLATFORM_`.
///
public enum PlatformActionIdentifiers implements IActionIdentifier {

    PLATFORM_COPY_USER_ROLE
    ;

}
